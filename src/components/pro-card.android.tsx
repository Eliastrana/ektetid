/**
 * No store on this platform — see pro-card-earned.tsx for why.
 *
 * Re-exported rather than duplicated so the two non-Apple platforms cannot
 * drift apart.
 */
export { ProCard } from '@/components/pro-card-earned';
